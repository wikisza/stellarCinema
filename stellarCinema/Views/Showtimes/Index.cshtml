@model IEnumerable<stellarCinema.Entities.Showtime>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hall)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShowtimeDateStart)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hall.HallName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShowtimeDateStart)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdShowtime">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdShowtime">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdShowtime">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
